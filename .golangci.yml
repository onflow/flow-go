run:
  timeout: 5m
  skip-dirs:
    - consensus/hotstuff
    
linters-settings:
  goimports:
    # put imports beginning with prefix after 3rd-party packages;
    # it's a comma-separated list of prefixes
    local-prefixes: github.com/dapperlabs/flow-go

linters:
  enable:
    - goimports
