language: go
go_import_path: github.com/dapperlabs/flow-go

branches:
  only:
  - master
  
cache:
  bundler: true
  directories:
    - $HOME/.cache/go-build
    - $HOME/gopath/pkg/mod

script:
  - make ci
