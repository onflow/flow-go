package reader

// TODO: Collections are queried by ID, or by transaction ID. Many use cases do not know which block the collection exists in,
//       so all methods should allow traversing the tree.

type Collections struct {
}
