package reader

// TODO: Similarly, all of the tx results error messages methods require a block ID. API endpoints only query error messages
//       for a single block, so traversal is not required.

type TransactionResultErrorMessages struct {
}
